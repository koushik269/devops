# Development Environment Variables
NODE_ENV=development
PORT=3001

# Database
DATABASE_URL="postgresql://vpsuser:vpspassword@localhost:5432/vps_seller_portal?schema=public"

# JWT Configuration
JWT_SECRET="dev-jwt-secret-change-in-production"
JWT_REFRESH_SECRET="dev-refresh-secret-change-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Frontend URL
FRONTEND_URL="http://localhost:3000"

# Redis Configuration
REDIS_URL="redis://localhost:6379"

# Email Configuration (MailHog for development)
SMTP_HOST="localhost"
SMTP_PORT=1025
SMTP_USER=""
SMTP_PASS=""
FROM_EMAIL="noreply@vpsportal.com"
FROM_NAME="VPS Seller Portal (Dev)"

# Bcrypt rounds (lower for development)
BCRYPT_ROUNDS=10

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Stripe (test keys)
STRIPE_SECRET_KEY="sk_test_example_key"
STRIPE_PUBLISHABLE_KEY="pk_test_example_key"
STRIPE_WEBHOOK_SECRET="whsec_example_webhook_secret"

# PayPal (sandbox)
PAYPAL_CLIENT_ID="test_paypal_client_id"
PAYPAL_CLIENT_SECRET="test_paypal_client_secret"
PAYPAL_MODE="sandbox"

# Coinbase Commerce (test)
COINBASE_COMMERCE_API_KEY="test_coinbase_api_key"
COINBASE_COMMERCE_WEBHOOK_SECRET="test_webhook_secret"

# Proxmox (placeholder for development)
PROXMOX_API_URL="https://proxmox-dev:8006/api2/json"
PROXMOX_TOKEN_ID="dev-token-id"
PROXMOX_TOKEN_SECRET="dev-token-secret"
PROXMOX_NODE="dev-node"

# Terraform (placeholder for development)
TERRAFORM_API_TOKEN="dev-terraform-token"
TERRAFORG_WORKSPACE="vps-dev-workspace"